import 'dart:ui';

  ///El archivo `constand` contiene los valores de colores utilizados en la UI del proyecto,
  ///los colores son proporcionados por la dependencia.

const greenColor = Color(0xFF255946); // Color para algunos componentes de la UI

const darkBackground = Color(0xFFBF8F54); // Color de fondo oscuro de la UI

const ligthBackground = Color(0xFFD9BC9A); // Color de fondo claro de la UI

const ligth = Color(0xFFF2E8DC); // Color claro de la UI

const dark = Color(0xFF595756); // Color oscuro de la UI