import 'package:flutter/cupertino.dart';

class CursosNormal extends StatelessWidget {
  const CursosNormal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Cursos");
  }
}
